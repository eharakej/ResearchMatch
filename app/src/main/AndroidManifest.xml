<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.researchmatch">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SimpleFragmentActivity"></activity>
        <activity android:name=".RegisterActivityFaculty" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ProfileActivity" />
        <activity
            android:name=".CreatePostActivity"
            android:label="@string/title_activity_create_post"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".FacultyProfilePage" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".PostActivity" />
        <activity android:name=".QuestionareActivity"/>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.qualtrics.digital.QualtricsSurveyActivity" />
        <activity
            android:name="com.qualtrics.digital.QualtricsPopOverActivity"
            android:theme="@style/Theme.Transparent" />
    </application>

</manifest>